# ------------------------------------------------------------------
# Landing page institucional — não usa banco, nem chaves de serviço.
# Copie para .env.local se for rodar com domínio próprio.
# ------------------------------------------------------------------

# URL pública do site. Entra no JSON-LD, no sitemap e no robots.
# Na Vercel é detectada sozinha; defina apenas se usar domínio próprio.
NEXT_PUBLIC_SITE_URL=
